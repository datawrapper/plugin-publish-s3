<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <p class="lead" style="margin:0">{% trans "Export to static image for printing" %}</p>
</div>
<div class="modal-body">
    <div class="row">
        <div class="span1" style="text-align:center">
            <i class="big fa fa-cloud-upload"></i>
        </div>
        <div class="span5">
            <p><a href="{{ chartUrl }}">{{ chartUrl }}</a></p>

            <div class="publish-progress hidden">
                {# show chart publish progress #}
                <div class="alert alert-info publishing">
                    <p>
                        <img src="/static/img/ajax-loader.gif" /> {% trans "Your chart is now being uploaded, and should be ready in a minute." %} 
                        <div class="progress progress-striped active">
                            <div class="bar" style="width: 0%;"></div>
                        </div>
                    </p>
                </div>
            </div>

            <div class="publish-success hidden">
                <p class="published alert alert-success">{% trans "Congrats, your chart has been published." %}</p>
            </div>

            <div class="republish-note hidden">
                {# show link to re-publish #}
                <div class="alert alert-warning">
                    <p>{% trans "You have changed the chart after its last publication. To make these changes show up you got to publish the chart again." %}</p>
                    <a href="#republish" class="btn btn-republish btn-primary"><i class="fa fa-refresh"></i> {% trans "Re-publish chart!" %}</a>
                </div>
            </div>
            
            {# show warning that chart url has changed #}
            {% if republish %}
                <div id="chart-url-change-warning" class="alert alert-warning" style="display:none">
                    <button type="button" class="close" data-dismiss="alert">&times;</button>
                    <div style="font-size:45px;line-height:55px;margin-left:-7px" class="pull-left">☝</div>
                    <div style="margin-left: 40px">
                        <h4>{% trans "Chart URL has changed!" %}</h4>
                        <div>{% trans "The chart URL has changed due to the republication. The old versions will be redirected automatically within the next 24 hours." %}</div>
                    </div>
                </div>
            {% endif %}

            {# show embed link #}
            <h4>{% trans "Embed into your website" %}</h4>
            <p>{% trans "Just copy &amp; paste the following code into your website." %}</p>
            <form class="form-inline">
                <label>{% trans "Width" %}: <input type="text" id="embed-width" class="input-small" style="width:4ex" value="600" /> px</label>&nbsp;&nbsp;
                <label>{% trans "Height" %}: <input type="text" id="embed-height" class="input-small" style="width:4ex" value="400" /> px</label>


            <h4>{% trans "Copy Embed Code" %}</h4>

            <textarea style="font-size: 12px;padding:5px;white-space: nowrap;font-family: Consolas, monospace;color: #777;background: #fbfbfb;" id="iframe-code" class="span3" rows="1"><iframe src="{{ chartUrl }}" frameborder="0" allowtransparency="true" allowfullscreen="allowfullscreen" webkitallowfullscreen="webkitallowfullscreen" mozallowfullscreen="mozallowfullscreen" oallowfullscreen="oallowfullscreen" msallowfullscreen="msallowfullscreen" width="{{ chart.metadata.publish['embed-width'] }}" height="{{ chart.metadata.publish['embed-height'] }}"></iframe></textarea><button class="btn" style="margin: 0 1ex" id="copy-button" data-clipboard-text="Copy Me!" title="Click to copy me."><i class="fa fa-files-o"></i> {% trans "Copy" %}</button><span class="hidden copy-success">{% trans "copied!" %}</span>

            <div style="color: #999;margin-top:20px"><b>{% trans "Note" %}:</b> {% trans "It may take a couple of minutes until the re-direction from old chart urls to the newest version takes effect." %}</div>

        </div>
    </div>


</div>
<div class="modal-footer">
   <button class="btn-publish btn pull-left">{% trans "Re-publish now" %}</button>
   <button class="btn" data-dismiss="modal" aria-hidden="true">{% trans "Done" %}</button>
</div>




